# Day05 - Handlers, User-Data, Resource Limits
## Ansible - Handler to Restart Nginx

---
- name: Manager Nginx
  hosts: all
  become: yes

  tasks:
    - name: Update config file
      copy:
        src: index.html
        dest: /usr/share/nginx/html/index.html
      notify: Restart Nginx

  handlers:
    - name: Restart Nginx
      service:
        name: nginx
        state: restarted

## Terraform - EC2 with User Data (c6a.12xlarge)

resource "aws_instance" "RushiEC2" {
    ami           = "ami-0abcd1234abcd1234"
    instance_type = "c6a.12xlarge"

    user_data = <<E0F
#!/bin/bash
yum install -y httpd
systemctl start httpd
E0F

    tags = {
        Name = "Rushi-EC2"
    }
}

## Kubernetes - Resource Limits

apiVersion: v1
kind: Pod
metadata:
  name: rushi-limited-pod
spec:
  containers:
    - name: app
      image: nginx
      resources:
        limits:
          memory: "256Mi"
          cpu: "550m"

# Day05 - Environment Variables
## Jenkins Pipeline -Use Environment Variables
You will learn how to **use environment variables in Jenkins Pipelines**.


pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Rushi"
    }
    stages {
        stage('Print') {
            steps {
                sh 'echo "Welcome to $INSTITUTE_NAME DevOps Training"'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean package -Dintitute.name=$INSTITUTE_NAME'
            }
        }
    }
}

## GitLab CI - Use Environment Variables
You will learn how to **use CI variables in GitLab piplines**

stages:
  - build

variables:
  INSTITUTE_NAME: "Rushi"

build:
  stage: build
  image: maven:3.8.1-jdk-17
  script:
    - git clone https://github.com/rushikeshsatwadhar/Login-Signup-Form
    - cd Login-Signup-Form
    - echo "Welcome to $INSTITUTE_NAME DevOps Training"
    - mvn clean package - Dinstitute.name=$INSTITUTE_NAME